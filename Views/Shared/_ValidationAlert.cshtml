@if (!ViewData.ModelState.IsValid && ViewData.ModelState.ErrorCount > 0)
{
    <div class="alert alert-danger soft-alert mb-3" role="alert">
        Please review the validation errors below and try again.
    </div>
}
